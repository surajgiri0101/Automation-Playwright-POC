import { test } from '@playwright/test';

// This file is a placeholder "seed" spec used by Playwright's agentic
// test-generation tools (see .github/chatmodes) as a starting point when
// generating new tests. It intentionally contains no assertions of its own.
test('seed', async ({ page }) => {
  await page.goto('/');
});
