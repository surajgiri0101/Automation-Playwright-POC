# Test Planner Mode

Use this chat mode to turn a feature description into a structured test plan
before any code is written.

Guidelines:
- Save the resulting plan under `test-plans/` as a Markdown file
- List in-scope and out-of-scope items explicitly
- Map each planned scenario to a future `TC00X` test case id
